1. List creation and modifications.
Ans:

list1 = [1,2,3,4,5]
list2 = ['Hema','Anu','Niru','Sesha']

#appending 6 to list1
list1.append('6')
print(list1)

#sort list2
list2.sort()
print(list2)

#insert an element 100 in list1 at position 3
list1.insert(2,100)
print(list1)

#Remove 100 from list1
list1.remove(100)
print(list1)

#pop from list2
list2.pop()
print(list1)


2. Create a calculator program in python.
Ans:

def add(x, y):
    return x + y

def sub(x, y):
    return x - y

def mul(x, y):
    return x * y

def div(x, y):
    return x / y

print("Choose option : 1.Add 2.Subtract 3.Multiply 4.Divide 5.Exit")
while True:
    print("Choose option : 1.Add 2.Subtract 3.Multiply 4.Divide 5.Exit")
    choice = input()
    if choice in ('1', '2', '3', '4'):
        num1 = float(input("Enter num1"))
        num2 = float(input("Enter num2"))

        if choice == '1':
            print(num1, "+", num2, "=", add(num1, num2))

        elif choice == '2':
            print(num1, "-", num2, "=", sub(num1, num2))

        elif choice == '3':
            print(num1, "*", num2, "=", mul(num1, num2))

        elif choice == '4':
            print(num1, "/", num2, "=", div(num1, num2))
    else:
        print("Exiting")
		break

3. String concatenation and slicing.
Ans:

String concatenation:
a = "Hello"
b = "World"
c = a + b
d = a + " " + b
print(c) #prints "HelloWorld"
print(d) #prints "Hello World"

Slicing:
d = "Hello World"
print(d[2:7]) #prints "llo W"



4. Why python is a popular language? 
Ans:
Python is a popular language compare to its contemporaries because its syntax is simple and beginner friendly and is easily understandable by both the developers and bussiness people because of the language's emphasis on natural language.
It also comes with a variety of libraries which can be used to simplify a lot of tasks and problem statements. These libraries can be easily downloaded through pip tool.
Python is also extensively used for Web Development purposes making it more popular.
Python is used in different domains like AI, ML, IOT, Web development etc making it very versatile.


5. What are the other frameworks that can be used with python?
Ans:
1) Django
2) Flask
3) Pyramid
4) Bottle
5) Web2Py 

        
6. WSGI full-form?
Ans:
Web Server Gateway Interface


